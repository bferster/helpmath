# HelpMath >> README.md
git init
git config --global user.name bferster
git config --global user.email bferster@stagetools.com

echo # HelpMath
# HelpMath >> README.md
git init
git config --global user.name bferster
git config --global user.email bferster@stagetools.com

echo # HelpMath
